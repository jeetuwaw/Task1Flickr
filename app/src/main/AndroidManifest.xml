<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="task.home.com.task1">

    <supports-screens
        android:anyDensity="true"
        android:compatibleWidthLimitDp="1000"
        android:largeScreens="true"
        android:largestWidthLimitDp="2000"
        android:normalScreens="true"
        android:requiresSmallestWidthDp="250"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".activitys.MainActivity"
            android:configChanges="keyboard|orientation|keyboardHidden|locale|smallestScreenSize|screenSize|screenLayout|colorMode|density|fontScale|layoutDirection|navigation|touchscreen|mcc|mnc|uiMode"
            android:label="@string/screen1"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activitys.FullScreenActivity"
            android:configChanges="keyboard|orientation|keyboardHidden|locale|smallestScreenSize|screenSize|screenLayout|colorMode|
density|fontScale|layoutDirection|navigation|touchscreen|mcc|mnc|uiMode"
            
            android:label="@string/screen2"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>